# n8n localhost — simple static server

This project serves `indexn8nbot.html` as the root index using a small Node/Express server.

Quick start:

1. Install dependencies:

```bash
npm install
```

2. Start the server:

```bash
npm start
```

3. Open in your browser:

http://localhost:3000
